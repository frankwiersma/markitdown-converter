# Environment Configuration for MarkItDown Converter

# Application Environment
# Options: development, production
QUART_ENV=development

# OpenAI API Configuration
# Optional: If not provided, users will need to enter their own API key
OPENAI_API_KEY=your-openai-api-key-here

# Server Configuration (Optional - defaults will be used if not set)
# HOST=0.0.0.0
# PORT=8080

# File Upload Configuration (Optional - defaults will be used if not set)
# MAX_CONTENT_LENGTH=16777216  # 16MB in bytes
# REQUEST_TIMEOUT=10

# Clever Cloud Deployment Variables (for production)
# CC_PRE_BUILD_HOOK=uv sync
# CC_RUN_COMMAND=uv run uvicorn app:app --host 0.0.0.0 --port 9000 --workers 4